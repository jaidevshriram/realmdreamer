from setuptools import setup, find_packages

setup(
    name="depth_anything",
    version="0.1",
    packages=["depth_anything"],
    install_requires=[
        # List your package's dependencies here, as strings
        # e.g., 'requests >= 2.20.0', 'numpy >= 1.18.1'
    ],
    # Consider other useful metadata:
    url="https://github.com/LiheYoung/Depth-Anything/",
)
